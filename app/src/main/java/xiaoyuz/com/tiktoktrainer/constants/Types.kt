package xiaoyuz.com.tiktoktrainer.constants

enum class Mode {
    TIME_MODE, COUNT_MODE, CUSTOM_MODE
}

enum class ScheduleType {
    GET_READY, REST, FIGHTING
}

enum class ProgressStatus {
    GET_READY, WORK_OUT, REST_NOW, PAUSE
}